#!/bin/sh

#
# ~/local/bin/shutdown
#

CMD="poweroff"
SYSTEMCTL="$(which systemctl 2>/dev/null)"

if [ -n "$SYSTEMCTL" ]
then
	$SYSTEMCTL "$CMD"
else
	echo "systemctl command not available."
	exit 127
fi