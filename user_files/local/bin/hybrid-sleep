#!/bin/sh

#
# ~/local/bin/hybrid-sleep
#

CMD="hybrid-sleep"
LOCK="lock_screen"
SYSTEMCTL="$(which systemctl 2>/dev/null)"

if [ -n "$SYSTEMCTL" ]
then
	$SYSTEMCTL "$CMD"
	$LOCK
else
	echo "systemctl command not available."
	exit 127
fi