#!/bin/sh

#
# ~/local/bin/hibernate
#

CMD="hibernate"
LOCK="lock_screen"
SYSTEMCTL="$(which systemctl 2>/dev/null)"

if [ -n "$SYSTEMCTL" ]
then
	$SYSTEMCTL "$CMD"
	$LOCK
else
	echo "systemctl command not available."
	exit 127
fi